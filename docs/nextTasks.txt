Currently we have

🔁 1. Updated Flow (Target UX)

index (user selects options)
   ↓
preWorkoutOverview (NEW screen)
   ↓
dynamicWarmUp (optional)
   ↓
workout (required)
   ↓
staticStretch (optional)
   ↓
workoutSummary


2. 👉 Treat EVERYTHING as a “Workout Block”


3. Index Screen Changes (User Selection)

const [includeWarmup, setIncludeWarmup] = useState(true);
const [includeStretch, setIncludeStretch] = useState(true);

Done but still need interface so user can toggl;e options on and off."DuoLingo" style with Dynamic warmuos highly recommended and static Stretches recommended

📋 4. NEW Screen: preWorkoutOverview.tsx

Done, but some style enhancements needed here

exercise routine heading should be in a different font, colour to the exercises listings. (must be done using / updating styles/appStyles

5. Estimate durations 

Is wrong and needs to be done

Each exercise needs:
type Exercise = {
  id: string;
  name: string;
  sets?: number;
  reps?: string;
  duration?: number; // seconds
  rest?: number;
};

A. calculation of estimated time
each exercise will have a default value of 2.5 seconds

exercise duration 
type :
reps
duration * reps * sets + (rests between sets * sets)

hold
hold * sets + (rests between sets * sets)

tempo
(eccentric + pauseEccentric +  concentric + pauseConcentric) * reps  +  (rests between sets * sets)

Duration of exercise routine
Duration for each exercises routine (warmup, main and stretch) 
will include estimated exercise duration above + rest between exercises * number of exercises.

Total duration estimation
sum of all duration of exercise routines

PS we will also need to calculate total actual time of workoutSummary
Probably need a clock start (when user starts the workoutSummaryand clock time when user completes the whole workout)  for later detais stats etc

🎥 5. Video Integration (JSON Layer)

Pending 

Lets handle the above points first

attached project.zip my current code and structure (tree.html)













